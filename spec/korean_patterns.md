# 한국어 허용 문장 패턴 (고정 200개)

아래 패턴들은 한국어 전용 허용 문장 패턴의 고정 목록이다.

1. 모듈 <모듈명>
2. 모듈을 <모듈명>
3. 모듈 <모듈명>
4. 사용 <모듈명>
5. 사용한다 <모듈명>
6. 모듈을 <모듈명> 별칭 <별칭>
7. 모듈을 <모듈명> 별칭으로 <별칭>
8. 모듈 <모듈명> 별칭으로 <별칭>
9. 사용 <모듈명> 별칭 <별칭>
10. 사용 <모듈명> 별칭으로 <별칭>
11. 사용한다 <모듈명> 별칭으로 <별칭>
12. 모듈을 "<경로>"
13. 모듈 "<경로>"
14. 사용 "<경로>"
15. 모듈을 "<경로>" 별칭으로 <별칭>
16. 사용 "<경로>" 별칭으로 <별칭>
17. 모듈을 <모듈명> 별칭으로 <짧은이름>
18. 모듈 <모듈명> 별칭 <짧은이름>
19. 사용 <모듈명> 별칭 <짧은이름>
20. 사용한다 <모듈명> 별칭 <짧은이름>
21. 모듈을 <모듈명> 별칭으로 <모듈별칭>
22. 모듈 <모듈명> 별칭으로 <모듈별칭>
23. 사용 <모듈명> 별칭으로 <모듈별칭>
24. 사용한다 <모듈명> 별칭으로 <모듈별칭>
25. 모듈을 <모듈명> 별칭으로 <이름>
26. 사용 <모듈명> 별칭으로 <이름>
27. 사용한다 <모듈명> 별칭으로 <이름>
28. 모듈 <모듈명> 별칭으로 <이름>
29. 모듈을 <모듈명> 별칭 <이름>
30. 사용 <모듈명> 별칭 <이름>
31. 함수를 정의한다 <함수명>은 아무것도 받지 않고 <반환타입>를 반환한다:
32. 함수를 정의한다 <함수명>은 아무것도 받지 않고 <반환타입>을 반환한다:
33. 함수를 정의한다 <함수명>은 <인자목록>을 받고 <반환타입>를 반환한다:
34. 함수를 정의한다 <함수명>은 <인자목록>을 받고 <반환타입>을 반환한다:
35. 함수를 정의한다 <함수명>은 <인자목록>를 받고 <반환타입>을 반환한다:
36. 함수를 정의한다 <함수명>은 <인자목록>를 받고 <반환타입>를 반환한다:
37. 공개 함수를 정의한다 <함수명>은 아무것도 받지 않고 <반환타입>를 반환한다:
38. 공개 함수를 정의한다 <함수명>은 아무것도 받지 않고 <반환타입>을 반환한다:
39. 공개 함수를 정의한다 <함수명>은 <인자목록>을 받고 <반환타입>를 반환한다:
40. 공개 함수를 정의한다 <함수명>은 <인자목록>을 받고 <반환타입>을 반환한다:
41. 비공개 함수를 정의한다 <함수명>은 아무것도 받지 않고 <반환타입>를 반환한다:
42. 비공개 함수를 정의한다 <함수명>은 아무것도 받지 않고 <반환타입>을 반환한다:
43. 비공개 함수를 정의한다 <함수명>은 <인자목록>을 받고 <반환타입>를 반환한다:
44. 비공개 함수를 정의한다 <함수명>은 <인자목록>을 받고 <반환타입>을 반환한다:
45. 함수를 정의한다 <함수명>은 <인자목록>을 받고 <반환타입>을 반환한다:
46. 함수를 정의한다 <함수명>은 <인자목록>을 받고 <반환타입>를 반환한다:
47. 공개 함수를 정의한다 <함수명>은 <인자목록>를 받고 <반환타입>을 반환한다:
48. 공개 함수를 정의한다 <함수명>은 <인자목록>를 받고 <반환타입>를 반환한다:
49. 비공개 함수를 정의한다 <함수명>은 <인자목록>를 받고 <반환타입>을 반환한다:
50. 비공개 함수를 정의한다 <함수명>은 <인자목록>를 받고 <반환타입>를 반환한다:
51. 함수를 정의한다 <함수명>은 아무것도 받지 않고 정수를 반환한다:
52. 함수를 정의한다 <함수명>은 아무것도 받지 않고 불리언을 반환한다:
53. 함수를 정의한다 <함수명>은 아무것도 받지 않고 문자열을 반환한다:
54. 함수를 정의한다 <함수명>은 아무것도 받지 않고 버퍼를 반환한다:
55. 함수를 정의한다 <함수명>은 아무것도 받지 않고 뷰를 반환한다:
56. 함수를 정의한다 <함수명>은 아무것도 받지 않고 텐서를 반환한다:
57. 함수를 정의한다 <함수명>은 아무것도 받지 않고 채널을 반환한다:
58. 함수를 정의한다 <함수명>은 아무것도 받지 않고 벡터를 반환한다:
59. 함수를 정의한다 <함수명>은 아무것도 받지 않고 없음을 반환한다:
60. 공개 함수를 정의한다 <함수명>은 아무것도 받지 않고 정수를 반환한다:
61. 공개 함수를 정의한다 <함수명>은 아무것도 받지 않고 문자열을 반환한다:
62. 비공개 함수를 정의한다 <함수명>은 아무것도 받지 않고 정수를 반환한다:
63. 비공개 함수를 정의한다 <함수명>은 아무것도 받지 않고 문자열을 반환한다:
64. 함수를 정의한다 <함수명>은 정수 인자를 받고 정수를 반환한다:
65. 함수를 정의한다 <함수명>은 문자열 인자를 받고 문자열을 반환한다:
66. 함수를 정의한다 <함수명>은 정수와 정수를 받고 정수를 반환한다:
67. 함수를 정의한다 <함수명>은 문자열과 정수를 받고 문자열을 반환한다:
68. 공개 함수를 정의한다 <함수명>은 정수 인자를 받고 정수를 반환한다:
69. 공개 함수를 정의한다 <함수명>은 문자열 인자를 받고 문자열을 반환한다:
70. 비공개 함수를 정의한다 <함수명>은 정수 인자를 받고 정수를 반환한다:
71. 비공개 함수를 정의한다 <함수명>은 문자열 인자를 받고 문자열을 반환한다:
72. 외부 함수 <함수명>(<인자목록>) -> <반환타입>
73. 외부 함수 <함수명>() -> <반환타입>
74. 공개 외부 함수 <함수명>(<인자목록>) -> <반환타입>
75. 비공개 외부 함수 <함수명>(<인자목록>) -> <반환타입>
76. 외부 함수 <함수명>(정수) -> 정수
77. 외부 함수 <함수명>(문자열) -> 문자열
78. 외부 함수 <함수명>(채널, 정수) -> 없음
79. 외부 함수 <함수명>(벡터, 정수) -> 정수
80. 외부 함수 <함수명>(버퍼) -> 뷰
81. 외부 함수 <함수명>(뷰) -> 없음
82. 공개 외부 함수 <함수명>() -> 정수
83. 공개 외부 함수 <함수명>() -> 문자열
84. 비공개 외부 함수 <함수명>() -> 정수
85. 비공개 외부 함수 <함수명>() -> 문자열
86. 외부 함수 <함수명>(정수, 정수) -> 정수
87. 외부 함수 <함수명>(문자열, 문자열) -> 문자열
88. 외부 함수 <함수명>(정수, 문자열) -> 문자열
89. 외부 함수 <함수명>(정수) -> 불리언
90. 외부 함수 <함수명>(불리언) -> 불리언
91. 외부 함수 <함수명>(정수, 정수, 정수) -> 정수
92. "<문자열>"를 출력한다
93. <값>을 출력한다
94. <값>를 출력한다
95. <이름>을 출력한다
96. <이름>를 출력한다
97. <표현식>을 출력한다
98. <표현식>를 출력한다
99. <함수호출>을 출력한다
100. <함수호출>를 출력한다
101. 반환한다
102. <값>을 반환한다
103. <값>를 반환한다
104. <이름>을 반환한다
105. <이름>를 반환한다
106. <표현식>을 반환한다
107. <표현식>를 반환한다
108. 0을 반환한다
109. 1을 반환한다
110. 참을 반환한다
111. 거짓을 반환한다
112. <대상>을 <값>로 설정한다
113. <대상>를 <값>로 설정한다
114. <대상>을 <값>으로 설정한다
115. <대상>를 <값>으로 설정한다
116. <이름>을 <값>로 설정한다
117. <이름>를 <값>로 설정한다
118. <이름>을 <값>으로 설정한다
119. <이름>를 <값>으로 설정한다
120. <대상>을 0으로 설정한다
121. <대상>을 1로 설정한다
122. <대상>을 참으로 설정한다
123. <대상>을 거짓으로 설정한다
124. <대상>을 "<문자열>"로 설정한다
125. <대상>를 "<문자열>"로 설정한다
126. <대상>을 <함수호출>로 설정한다
127. <대상>를 <함수호출>로 설정한다
128. <대상>을 <표현식>으로 설정한다
129. <대상>를 <표현식>으로 설정한다
130. <대상>에 <값>을 더한다
131. <대상>에 <값>를 더한다
132. <대상>에 1을 더한다
133. <대상>에 2를 더한다
134. <대상>에 <이름>을 더한다
135. <대상>에 <이름>를 더한다
136. <대상>에 <표현식>을 더한다
137. <대상>에 <표현식>를 더한다
138. <이름>에 <값>을 더한다
139. <이름>에 <값>를 더한다
140. <이름>에 1을 더한다
141. <이름>에 <함수호출>를 더한다
142. 만약 <조건>이면:
143. <조건>이면:
144. 만약 참이면:
145. 만약 거짓이면:
146. <이름>이면:
147. <표현식>이면:
148. 만약 <비교식>이면:
149. <비교식>이면:
150. 동안 <조건>:
151. 동안 참:
152. 동안 거짓:
153. 동안 <비교식>:
154. <횟수>번 반복한다:
155. 1번 반복한다:
156. 2번 반복한다:
157. 10번 반복한다:
158. <이름>번 반복한다:
159. <표현식>번 반복한다:
160. 중단한다
161. 계속한다
162. <버퍼명>을 <크기>바이트로 생성한다
163. <버퍼명>을 1바이트로 생성한다
164. <버퍼명>을 64바이트로 생성한다
165. <뷰명>를 <버퍼명>의 <시작>부터 <끝>까지로 빌려온다(불변)
166. <뷰명>를 <버퍼명>의 <시작>부터 <끝>까지로 빌려온다(가변)
167. <뷰명>를 <버퍼명>의 0부터 <끝>까지로 빌려온다(불변)
168. <뷰명>를 <버퍼명>의 0부터 <끝>까지로 빌려온다(가변)
169. <뷰명>를 <버퍼명>의 <시작>부터 <끝>까지로 빌려온다(불변)
170. <뷰명>를 <버퍼명>의 <시작>부터 <끝>까지로 빌려온다(가변)
171. 소유권을 이동한다 <원본> -> <대상>
172. 소유권을 이동한다 <이름> -> <이름>
173. <대상>를 해제한다
174. <대상>을 해제한다
175. <버퍼명>을 해제한다
176. <채널명>을 해제한다
177. <텐서명>를 해제한다
178. <문자열명>을 해제한다
179. <벡터명>를 해제한다
180. 빌려온다(불변) <대상>
181. 빌려온다(가변) <대상>
182. <대상>을 복사한다 <원본>로 설정한다
183. <대상>를 복사한다 <원본>로 설정한다
184. <대상>을 복사한다 <원본>으로 설정한다
185. <대상>를 복사한다 <원본>으로 설정한다
186. <대상>을 빌려온다(불변) <원본>로 설정한다
187. <대상>를 빌려온다(불변) <원본>로 설정한다
188. <대상>을 빌려온다(가변) <원본>로 설정한다
189. <대상>를 빌려온다(가변) <원본>로 설정한다
190. <대상>을 빌려온다(불변) <원본>으로 설정한다
191. <대상>를 빌려온다(불변) <원본>으로 설정한다
192. <대상>을 빌려온다(가변) <원본>으로 설정한다
193. <대상>를 빌려온다(가변) <원본>으로 설정한다
194. <대상>을 복사한다 <원본>으로 설정한다
195. <대상>를 복사한다 <원본>으로 설정한다
196. <대상>을 <왼값> + <오른값>로 설정한다
197. <대상>을 <왼값> - <오른값>로 설정한다
198. <대상>을 <왼값> * <오른값>로 설정한다
199. <대상>을 <왼값> / <오른값>로 설정한다
200. <대상>을 <왼값> == <오른값>로 설정한다
201. 시도 <표현식>
202. 시도 <함수호출>
203. 시도한다 <표현식>
204. 시도한다 <함수호출>
205. <표현식>을 시도한다

